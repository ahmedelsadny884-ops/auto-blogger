import os
import time
import random

# Dummy function for testing
def generate_article(index):
    return f"Title {index}: Test Article\nContent: This is a test article content for article {index}.\nImage URL: https://via.placeholder.com/600"

# عدد المقالات اليومية
articles_per_day = 10
# وقت الفاصل بين كل مقال بالثواني (3600 = ساعة)
time_between_articles = 3600

for i in range(1, articles_per_day + 1):
    article = generate_article(i)
    print(article)
    print("-"*50)
    # تعليق وقت الانتظار للتجربة السريعة
    # time.sleep(time_between_articles)
